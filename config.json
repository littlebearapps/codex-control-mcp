{
  "name": "mcp-delegator",
  "description": "Delegate AI agent tasks from Claude Code to Codex, Claude Code (Agent SDK), and more - with async execution",
  "version": "3.4.2",
  "tools": [
    {
      "name": "codex_run",
      "description": "Execute a Codex task in read-only mode (analysis, tests, etc.)",
      "modes": ["read-only", "full-auto", "danger-full-access"]
    },
    {
      "name": "codex_plan",
      "description": "Preview what Codex would do without executing"
    },
    {
      "name": "codex_apply",
      "description": "Execute file-modifying tasks (requires confirm=true)"
    },
    {
      "name": "codex_status",
      "description": "Get server status and active processes"
    }
  ],
  "security": {
    "maxConcurrency": 2,
    "secretRedaction": true,
    "inputValidation": true,
    "mutationGating": true
  }
}
