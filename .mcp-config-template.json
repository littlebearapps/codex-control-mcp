{
  "mcpServers": {
    "codex-control": {
      "command": "codex-control-mcp",
      "env": {
        "CODEX_MAX_CONCURRENCY": "2"
      },
      "description": "Hidden primitives for OpenAI Codex operations (local + cloud)"
    }
  }
}
